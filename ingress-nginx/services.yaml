apiVersion: v1
kind: Service
metadata:
  name: meuapp-stable
  namespace: default
spec:
  selector:
    app: meuapp
  ports:
    - port: 80
      targetPort: 5678
---
apiVersion: v1
kind: Service
metadata:
  name: meuapp-canary
  namespace: default
spec:
  selector:
    app: meuapp
  ports:
    - port: 80
      targetPort: 5678